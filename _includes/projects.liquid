<div class="col">
  <a href="{% if project.redirect %}{{ project.redirect }}{% else %}{{ project.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if project.img %}
        {%
          include figure.liquid
          loading="eager"
          path=project.img
          sizes = "250px"
          alt="project thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ project.title }}</h2>
        <p class="card-text">{{ project.description }}</p>
        {% if project.stack or project.tags %}
          <div class="project-tags">
            {% for item in project.stack %}
              <span class="project-tag project-tag-stack">{{ item }}</span>
            {% endfor %}
            {% for item in project.tags %}
              <span class="project-tag">{{ item }}</span>
            {% endfor %}
          </div>
        {% endif %}
        <div class="project-footer">
          {% if project.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ project.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if project.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i> {{ project.github_stars }}
                </span>
              {% endif %}
            </div>
          {% endif %}
          {% if project.updated %}
            <span class="project-updated" data-toggle="tooltip" title="Last Updated">
              <i class="fa-solid fa-clock"></i> {{ project.updated }}
            </span>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
