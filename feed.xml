<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://debajyotis.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://debajyotis.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-17T12:02:05+00:00</updated><id>https://debajyotis.github.io/feed.xml</id><title type="html">blank</title><subtitle>ML research engineer working on generative models, multimodal post-training, and scientific ML. PhD in Physics and Machine Learning, University of Geneva. Based in Geneva. </subtitle><entry><title type="html">One Uniform Distribution Is Enough, Until the Dimension Grows</title><link href="https://debajyotis.github.io/blog/2026/sampling-in-one-dimension/" rel="alternate" type="text/html" title="One Uniform Distribution Is Enough, Until the Dimension Grows"/><published>2026-08-17T00:00:00+00:00</published><updated>2026-08-17T00:00:00+00:00</updated><id>https://debajyotis.github.io/blog/2026/sampling-in-one-dimension</id><content type="html" xml:base="https://debajyotis.github.io/blog/2026/sampling-in-one-dimension/"><![CDATA[<p>I ended the last post with a perfect, uncorrelated stream of numbers in $[0,1)$, and promised that every sampler in this series would be built out of it. I should cash in on that promise, albeit for the easy case. I will do this for one dimension, and a target distribution that you already know how to write down. If you’re comfortable with CDFs, basic calculus, and the standard Gaussian, you have everything you need.</p> <p>Say you have $u \sim \mathcal{U}(0,1)$ and you want $x$ from some other distribution entirely, an exponential, a Gaussian, or a bimodal mess you can only evaluate pointwise. The question is how do you get there from a single uniform number?</p> <h2 id="flipping-the-cdf-politely">Flipping the CDF, politely</h2> <p>Every distribution has a CDF, defined as $F(x) = P(X \le x)$, and every CDF is a function that takes in $x$ and returns a number in $[0,1]$. What happens if you run it backwards? If $U \sim \mathcal{U}(0,1)$, then $X = F^{-1}(U)$ has exactly the distribution you wanted. This is the so-called the probability integral transform!</p> <p>Lets look at the exponential distribution as the concrete case, since its CDF can be inverted by hand. $F(x) = 1 - e^{-x}$, so $F^{-1}(u) = -\ln(1-u)$. Note that since $1-U$ is itself uniform on $(0,1)$, people usually just write $-\ln(u)$.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>

<span class="k">def</span> <span class="nf">sample_exponential</span><span class="p">(</span><span class="n">rng</span><span class="p">,</span> <span class="n">n</span><span class="p">):</span>
    <span class="n">u</span> <span class="o">=</span> <span class="n">rng</span><span class="p">.</span><span class="nf">uniform</span><span class="p">(</span><span class="n">size</span><span class="o">=</span><span class="n">n</span><span class="p">)</span>
    <span class="k">return</span> <span class="o">-</span><span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">u</span><span class="p">)</span>
</code></pre></div></div> <figure> <picture> <img src="/assets/img/sampling-series/inverse-cdf.svg" class="img-fluid rounded" width="100%" height="auto" alt="The exponential CDF F(x) = 1 - e^{-x} plotted with five horizontal uniform draws mapped across to the curve and down to the corresponding x values, illustrating inverse-CDF sampling." loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Five uniform draws on the vertical axis, mapped through the inverse CDF to exponential draws on the horizontal one. Notice how the x-values crowd near zero, exactly where the exponential density is highest.</figcaption> </figure> <p>The figure shows why this works. Where the CDF is steep, a small window of $u$ covers a wide window of $x$, so draws rarely land there. Places where the CDF is flat, the same window of $u$ covers a narrow window of $x$, so draws pile up. The steepness of $F$ is related to the density, and inverting it converts uniform coverage on the $u$-axis directly into the right coverage on the $x$-axis.</p> <p>Cool! So why doesn’t everyone just do this for everything? Because closed-form inverses are rare. Consider a simple counterexample. The Gaussian. Its CDF, $\Phi(x) = \tfrac12\left[1+\mathrm{erf}(x/\sqrt2)\right]$, has no elementary inverse. You can still get $\Phi^{-1}(u)$, but only by numerical approximation, and Gaussians are used often enough in Monte Carlo inner loops that this used to matter a lot.</p> <h2 id="a-neat-trick-for-gaussians">A neat trick for Gaussians</h2> <p>Although a single Gaussian’s CDF won’t invert, a pair of them will. We just need to move away from Cartesian coordinates. Take two independent standard normals $X, Y$. Their joint density is rotationally symmetric, so switch to polar: $R^2 = X^2+Y^2$ and $\theta = \arctan(Y/X)$. It turns out $\theta$ is uniform on $(0, 2\pi)$ and $R^2$ is exponential, and both of those invert by hand, the same trick as the section above. This is the famous <a href="https://projecteuclid.org/journals/annals-of-mathematical-statistics/volume-29/issue-2/A-Note-on-the-Generation-of-Random-Normal-Deviates/10.1214/aoms/1177706645.full">Box-Muller transform</a>, from Box and Muller’s 1958 paper:</p> \[R = \sqrt{-2\ln U_1}, \qquad \theta = 2\pi U_2, \qquad X = R\cos\theta, \quad Y = R\sin\theta.\] <p>Two uniforms in, two exact standard normals out, no approximation anywhere. The catch is the two transcendental calls, <code class="language-plaintext highlighter-rouge">sin</code> and <code class="language-plaintext highlighter-rouge">cos</code>, which used to be the expensive part of this whole operation on older hardware.</p> <p><a href="https://epubs.siam.org/doi/10.1137/1006063">George Marsaglia’s fix</a>, with Thomas Bray in 1964, removes them by getting $\cos\theta$ and $\sin\theta$ geometrically instead of trigonometrically. Draw a point $(u,v)$ uniformly on the square $[-1,1]^2$, and keep only the ones that land inside the unit circle, where $s = u^2+v^2 \in (0,1)$. For such a point, $u/\sqrt{s}$ and $v/\sqrt{s}$ already are the cosine and sine of its angle, for free, no trig function needed.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">sample_normal_pair</span><span class="p">(</span><span class="n">rng</span><span class="p">):</span>
    <span class="k">while</span> <span class="bp">True</span><span class="p">:</span>
        <span class="n">u</span><span class="p">,</span> <span class="n">v</span> <span class="o">=</span> <span class="n">rng</span><span class="p">.</span><span class="nf">uniform</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">size</span><span class="o">=</span><span class="mi">2</span><span class="p">)</span>
        <span class="n">s</span> <span class="o">=</span> <span class="n">u</span><span class="o">*</span><span class="n">u</span> <span class="o">+</span> <span class="n">v</span><span class="o">*</span><span class="n">v</span>
        <span class="k">if</span> <span class="mi">0</span> <span class="o">&lt;</span> <span class="n">s</span> <span class="o">&lt;</span> <span class="mi">1</span><span class="p">:</span>
            <span class="n">scale</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="o">-</span><span class="mi">2</span><span class="o">*</span><span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">s</span><span class="p">)</span><span class="o">/</span><span class="n">s</span><span class="p">)</span>
            <span class="k">return</span> <span class="n">u</span><span class="o">*</span><span class="n">scale</span><span class="p">,</span> <span class="n">v</span><span class="o">*</span><span class="n">scale</span>
</code></pre></div></div> <p>Notice what just happened. To dodge two trig calls, Marsaglia’s method throws darts at a square and keeps only the ones that land in the circle. The fraction it keeps is the circle’s area over the square’s, $\pi/4 \approx 0.785$. That’s a rejection sampler (see below)!</p> <h2 id="the-ziggurat">The Ziggurat</h2> <p>So, neither of the two methods above is what your machine actually calls when you run <code class="language-plaintext highlighter-rouge">np.random.normal()</code>. That’s the Ziggurat algorithm, from <a href="https://www.jstatsoft.org/article/view/v005i08">Marsaglia and Tsang’s 2000 paper</a>.</p> <p>The idea is to cover the density with a stack of horizontal rectangles of equal area, layered from a wide short one at the bottom to a tall thin sliver at the top, near the tail. Sampling then amounts to picking a random layer and a random horizontal position in it. Almost all such positions fall under the curve within that layer, and you can check that with one comparison and no function calls at all. Only the rare position that lands in the sliver where a rectangle pokes out past the true density needs a slower fallback. Since that’s a small fraction of draws, the amortized cost is close to one comparison and one multiply, faster than either Box–Muller or its polar cousin. This is what current numpy, and most serious scientific libraries, actually run under <code class="language-plaintext highlighter-rouge">standard_normal</code>.</p> <h2 id="rejection-sampling">Rejection sampling</h2> <p>Lets generalize what Marsaglia’s method was already doing, an idea <a href="https://mcnp.lanl.gov/pdf_files/InBook_Computing_1961_Neumann_JohnVonNeumannCollectedWorks_VariousTechniquesUsedinConnectionwithRandomDigits.pdf">von Neumann described in general form back in 1951</a>. Say you have a target density $p(x)$, maybe not even normalized, maybe known only up to a constant that you can’t compute. This is often the normal state of affairs in real life. You have a proposal $q(x)$ you know how to sample and evaluate, and a constant $M$ large enough that $p(x) \le M q(x)$ everywhere. Draw $x \sim q$, draw $u \sim \mathcal{U}(0,1)$ independently, and accept $x$ only if $u \le p(x)/(Mq(x))$.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">rejection_sample</span><span class="p">(</span><span class="n">rng</span><span class="p">,</span> <span class="n">p</span><span class="p">,</span> <span class="n">q_sample</span><span class="p">,</span> <span class="n">q_pdf</span><span class="p">,</span> <span class="n">M</span><span class="p">):</span>
    <span class="k">while</span> <span class="bp">True</span><span class="p">:</span>
        <span class="n">x</span> <span class="o">=</span> <span class="nf">q_sample</span><span class="p">(</span><span class="n">rng</span><span class="p">)</span>
        <span class="n">u</span> <span class="o">=</span> <span class="n">rng</span><span class="p">.</span><span class="nf">uniform</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">u</span> <span class="o">&lt;=</span> <span class="nf">p</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="n">M</span> <span class="o">*</span> <span class="nf">q_pdf</span><span class="p">(</span><span class="n">x</span><span class="p">)):</span>
            <span class="k">return</span> <span class="n">x</span>
</code></pre></div></div> <p>The accepted draws are distributed exactly as $p$, and the overall probability of accepting any given proposal is exactly $1/M$, independent of $x$:</p> \[P(\text{accept}) = \int q(x)\,\frac{p(x)}{Mq(x)}\,dx = \frac{1}{M}\int p(x)\,dx = \frac{1}{M}.\] <figure> <picture> <img src="/assets/img/sampling-series/rejection-sampling.svg" class="img-fluid rounded" width="100%" height="auto" alt="A bimodal target density p(x) with a wider Gaussian envelope M times q(x) drawn over it, and a scatter of sampled points showing which fall under p(x) and are accepted versus which fall between p(x) and the envelope and are rejected." loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Rejection sampling against a bimodal target, using a single wide Gaussian as the proposal. Points under the solid curve are kept; points above it but under the dashed envelope are thrown away. Here 1/M is about 41 percent, so well over half of every draw is wasted.</figcaption> </figure> <p>The whole method depends on how tight $M$ can be made, which is really a question of how well $q$’s shape matches $p$’s. If you bound a target with a proposal that’s much wider or a different shape, as in the figure, $M$ balloons, and $1/M$ collapses. Every draw you throw away costs exactly as much to generate as one you keep.</p> <h2 id="importance-sampling">Importance sampling</h2> <p>If you push $q$ far enough from $p$, $M$ becomes unknown, or so large that keeping even one out of a million proposals would be luck. Rejection sampling’s whole strategy is quite literally <em>discard what doesn’t fit</em>, which stops being viable in this case.</p> <p>Importance sampling keeps every draw and fixes the mismatch by reweighting instead! Lets draw $x_1, \dots, x_N$ from $q$, and instead of accepting or rejecting, attach each one a weight $w_i = \tilde p(x_i)/q(x_i)$, where $\tilde p$ is allowed to be an unnormalized version of your target. Then normalize the weights to sum to one and use them directly as an estimator:</p> \[\mathbb{E}_p[f(X)] \approx \sum_i \hat w_i\, f(x_i), \qquad \hat w_i = \frac{w_i}{\sum_j w_j}.\] <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">self_normalized_is</span><span class="p">(</span><span class="n">x_samples</span><span class="p">,</span> <span class="n">p_tilde</span><span class="p">,</span> <span class="n">q_pdf</span><span class="p">,</span> <span class="n">f</span><span class="p">):</span>
    <span class="n">w</span> <span class="o">=</span> <span class="nf">p_tilde</span><span class="p">(</span><span class="n">x_samples</span><span class="p">)</span> <span class="o">/</span> <span class="nf">q_pdf</span><span class="p">(</span><span class="n">x_samples</span><span class="p">)</span>
    <span class="n">w_hat</span> <span class="o">=</span> <span class="n">w</span> <span class="o">/</span> <span class="n">w</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>
    <span class="k">return</span> <span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">w_hat</span> <span class="o">*</span> <span class="nf">f</span><span class="p">(</span><span class="n">x_samples</span><span class="p">))</span>
</code></pre></div></div> <p>Look closely at that ratio and notice that any constant multiplying $\tilde p$, in particular whatever normalizing constant $Z$ you didn’t want to compute, cancels between the numerator and denominator! This is the first time in this series that the normalizer disappears for free rather than needing to be dodged with cleverness (it won’t be the last). Every family of generative model this series eventually covers is, in one way or another, working around the fact that $Z$ is usually the one quantity you cannot get your hands on!</p> <p>Reweighting instead of discarding sounds like it should be strictly better than rejection, and in one sense it is. You never throw a sample away outright. But a badly matched $q$ doesn’t disappear. It just relocates into the weights. If $q$ rarely falls in the region where $p$ actually lives, the few samples that do land there get enormous weights, and everything else gets a weight near zero. Your estimate is then effectively built from one or two data points.</p> <p>The standard way to see through the disguise is the effective sample size, a diagnostic Leslie Kish worked out for weighted survey estimates back in 1965: $\mathrm{ESS} = (\sum_i w_i)^2 / \sum_i w_i^2$. It ranges from $1$, one dominant weight carrying everything, up to $N$, every weight equal. It answers a concrete question: how many honest draws from $p$ would your $N$ weighted draws from $q$ be worth?</p> <p>Okay, a brief aside for completeness before the main event. Low-discrepancy sequences (<a href="https://eudml.org/doc/131448">Halton, 1960</a>; <a href="https://www.mathnet.ru/eng/zvmmf7334">Sobol, 1967</a>) replace i.i.d. uniform draws with deterministic ones engineered to fill space more evenly at every $N$, which tightens both rejection and importance sampling’s constants without changing their exponential dependence on dimension. It’s a real technique, quasi-Monte Carlo, but it postpones the problem in the next section instead of solving it.</p> <h2 id="cranking-up-the-dimension">Cranking up the dimension</h2> <p>Everything above was doing fine in one dimension. Watch what happens if I push it into $d$ dimensions.</p> <p>For rejection sampling, lets take the cleanest possible mismatch: target uniform inside the unit ball, proposal uniform on its bounding cube $[-1,1]^d$. The acceptance rate is the volume ratio of ball to cube. For importance sampling, take two Gaussians differing only slightly in scale, target $\mathcal{N}(0, 1.1^2 I_d)$ against proposal $\mathcal{N}(0, I_d)$, and track $\mathrm{ESS}/N$ as $d$ grows.</p> <figure> <picture> <img src="/assets/img/sampling-series/curse-of-dimension.svg" class="img-fluid rounded" width="100%" height="auto" alt="Two log-scale line plots side by side. Left: rejection sampling acceptance rate for a ball inside its bounding cube, dropping from 1 at dimension 1 to about 10^-10 by dimension 25. Right: effective sample size fraction for importance sampling between two Gaussians differing by 10 percent in standard deviation, dropping from near 1 at dimension 1 to under 2 percent by dimension 200." loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Both quantities are on a log axis. Rejection sampling's acceptance rate falls below one in a billion by d=25 for a mismatch you'd call negligible in 1D. Importance sampling survives longer, since the two Gaussians here differ by only 10 percent in scale, but the same exponential decay is visible by d=200.</figcaption> </figure> <p>The ball-in-cube acceptance rate is already down to 0.25 percent by $d=10$ and to $2.5\times10^{-8}$ by $d=20$. The importance sampling side degrades slower, since a 10 percent scale mismatch is a much gentler test than a ball versus a cube, but by $d=200$ the effective sample size has fallen to under 2 percent of $N$: two hundred thousand weighted draws behaving like four thousand real ones.</p> <p>This isn’t a quirk of these two toy setups by the way. <a href="https://www.stat.berkeley.edu/~bickel/SBBA2008_submitted.pdf">Snyder, Bengtsson, Bickel, and Anderson (2008)</a> worked out the general version of the importance sampling story for particle filters and found that the ensemble size needed to avoid weight collapse scales exponentially with the state dimension, and reported needing on the order of $10^{11}$ particles for a 200-dimensional problem with independent Gaussian errors, a regime not so different from the toy above.</p> <p>The mechanism behind both plots is the same one. A small per-coordinate mismatch, whether it’s a ball versus a cube’s corner, or a 10 percent difference in scale, gets multiplied across $d$ independent coordinates. Products of $d$ numbers each a little less than one shrink exponentially in $d$, whether or not any single coordinate looks like a problem. Dimension makes these methods fail on a scale no single coordinate’s plot would predict.</p> <p>I find this the most underrated fact in the foundations of Monte Carlo methods, more so than any individual algorithm in this post. Everything above works by drawing i.i.d. proposals and either keeping the lucky ones or weighting them by how lucky they got. In high dimension, being simultaneously lucky in every one of $d$ directions is not a rare event. It is, for practical purposes, not an event at all.</p> <p>This post is part of a series on sampling and generative modeling, working up from raw uniform bits to the machinery behind modern generative models. The next post drops the requirement that proposals be independent. A Markov chain that’s allowed to wander, correlated step after correlated step, spending its time in proportion to how much probability mass sits wherever it currently is, sidesteps the need to guess a global envelope or a global proposal altogether. That’s Metropolis–Hastings, and it’s the actual answer to the crisis in the figure above.</p>]]></content><author><name></name></author><category term="GenAI"/><category term="ml"/><category term="generative-modelling"/><category term="probability"/><category term="sampling"/><summary type="html"><![CDATA[Inverse-CDF, rejection sampling, and importance sampling all turn uniform bits into arbitrary distributions in one dimension. All three die exponentially in high dimension.]]></summary></entry><entry><title type="html">Your Random Number Generator Is Not Random</title><link href="https://debajyotis.github.io/blog/2026/random-number-generators/" rel="alternate" type="text/html" title="Your Random Number Generator Is Not Random"/><published>2026-08-14T00:00:00+00:00</published><updated>2026-08-14T00:00:00+00:00</updated><id>https://debajyotis.github.io/blog/2026/random-number-generators</id><content type="html" xml:base="https://debajyotis.github.io/blog/2026/random-number-generators/"><![CDATA[<p>Say you fire up your favourite programming language, and a suitable random number generator (RNG) package therein. Call <code class="language-plaintext highlighter-rouge">random.seed(42)</code> (or something equivalent to set the random seed), draw a number, restart the program, call <code class="language-plaintext highlighter-rouge">random.seed(42)</code> again, and you get the exact same number back. Do it a thousand times and you get the exact same thousand numbers, in the exact same order, forever. If you had to design a definition of “not random” from scratch, perfect on-demand reproducibility would be a good place to start.</p> <p>And yet this is the thing every probabilistic method in machine learning is built on. This post is for readers comfortable with Python and basic probability who have called a <code class="language-plaintext highlighter-rouge">seed()</code> function more times than they’ve asked what it does underneath. What it does, it turns out, is short, mechanical, because every other post in this series (every sampler, every Markov chain, every diffusion model’s noise, every flow’s base distribution) is going to be a function of what comes out of it.</p> <h2 id="a-generator-is-actually-just-a-short-program">A generator is actually just a short program</h2> <p>If you strip away the language bindings, a random number generator is simply a recurrence relation. The oldest and simplest useful one is the so called linear congruential generator:</p> \[X_{n+1} = (a X_n + c) \bmod m\] <p>Say you pick a multiplier $a$, an increment $c$, a modulus $m$, and a starting value $X_0$ called the seed. This fixes the whole future sequence. To turn an integer $X_n \in {0, \dots, m-1}$ into something that looks like a draw from $\mathcal{U}(0,1)$, you simply divide: $u_n = X_n / m$.</p> <p>Here’s a small enough example to run by hand. Take $a=5$, $c=3$, $m=16$, seed $X_0=1$:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">lcg</span><span class="p">(</span><span class="n">seed</span><span class="p">,</span> <span class="n">a</span><span class="p">,</span> <span class="n">c</span><span class="p">,</span> <span class="n">m</span><span class="p">,</span> <span class="n">n</span><span class="p">):</span>
    <span class="n">x</span> <span class="o">=</span> <span class="n">seed</span>
    <span class="n">out</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n</span><span class="p">):</span>
        <span class="n">x</span> <span class="o">=</span> <span class="p">(</span><span class="n">a</span> <span class="o">*</span> <span class="n">x</span> <span class="o">+</span> <span class="n">c</span><span class="p">)</span> <span class="o">%</span> <span class="n">m</span>
        <span class="n">out</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">out</span>

<span class="nf">lcg</span><span class="p">(</span><span class="n">seed</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">a</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span> <span class="n">c</span><span class="o">=</span><span class="mi">3</span><span class="p">,</span> <span class="n">m</span><span class="o">=</span><span class="mi">16</span><span class="p">,</span> <span class="n">n</span><span class="o">=</span><span class="mi">20</span><span class="p">)</span>
<span class="c1"># [8, 11, 10, 5, 12, 15, 14, 9, 0, 3, 2, 13, 4, 7, 6, 1, 8, 11, 10, 5]
</span></code></pre></div></div> <p>Notice that it generates sixteen(!) numbers, and then it repeats from the start. It visited every integer from 0 to 15 exactly once before cycling. This is not an accident. It’s a consequence of the particular $a$ and $c$ chosen (the Hull–Dobell theorem gives the exact conditions, if you were keen on following up), and it’s the best you could possibly ask of a generator with sixteen possible states. This is the entire mechanism. <code class="language-plaintext highlighter-rouge">numpy.random</code>, your language’s standard library, the generator inside PyTorch: all of it is this idea, with a modulus around $2^{64}$ or larger and considerably more care put into choosing $a$ and $c$.</p> <h2 id="a-generator-can-be-bad">A generator can be bad</h2> <p>So, a generator with modulus $m$ can produce at most $m$ distinct values before it repeats. Then the first thing you want is a <strong>long period</strong>: you should exhaust your Monte Carlo budget long before the sequence loops back on itself. That part is easy to check by construction.</p> <p>The harder property is <strong>equidistribution</strong>, i.e. single outputs $u_n$ should be spread uniformly over $[0,1)$, and so should <em>tuples</em> of consecutive outputs $(u_n, u_{n+1}, \dots, u_{n+d-1})$ over $[0,1)^d$, for every $d$ you might care about. A generator can pass every check you throw at single numbers and still be badly broken two or three draws at a time, because the recurrence that generates $u_{n+1}$ from $u_n$ leaves a fingerprint that only shows up when you look at them together.</p> <h2 id="case-study-randu">Case Study: RANDU</h2> <p>In the 1960s, IBM shipped a generator called RANDU with its scientific subroutine library. It’s a pure multiplicative generator (no increment) with</p> \[X_{n+1} = a X_n \bmod m, \qquad a = 65539, \quad m = 2^{31}.\] <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">randu</span><span class="p">(</span><span class="n">seed</span><span class="p">,</span> <span class="n">n</span><span class="p">):</span>
    <span class="n">a</span><span class="p">,</span> <span class="n">m</span> <span class="o">=</span> <span class="mi">65539</span><span class="p">,</span> <span class="mi">2</span><span class="o">**</span><span class="mi">31</span>
    <span class="n">x</span> <span class="o">=</span> <span class="n">seed</span>
    <span class="n">out</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n</span><span class="p">):</span>
        <span class="n">x</span> <span class="o">=</span> <span class="p">(</span><span class="n">a</span> <span class="o">*</span> <span class="n">x</span><span class="p">)</span> <span class="o">%</span> <span class="n">m</span>
        <span class="n">out</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">x</span> <span class="o">/</span> <span class="n">m</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">out</span>
</code></pre></div></div> <p>RANDU was the default generator on mainframes for over a decade, cited in a huge number of published Monte Carlo results. It looks completely fine if you plot pairs of consecutive draws $(u_n, u_{n+1})$: a uniform speckle over the unit square, no visible structure. The left panel below is four thousand such pairs.</p> <figure> <picture> <img src="/assets/img/sampling-series/randu-planes.svg" class="img-fluid rounded" width="100%" height="auto" alt="Left panel: a scatter plot of consecutive RANDU pairs (u_n, u_n+1) filling the unit square uniformly with no visible pattern. Right panel: the same draws plotted as u_n against the combination 9u_n - 6u_n+1 + u_n+2, collapsing onto exactly fifteen horizontal lines at integer heights from -5 to 9." loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Four thousand consecutive draws from RANDU. Pairs look uniform (left). The same points, viewed through the one combination RANDU can't hide, collapse onto exactly fifteen lines (right).</figcaption> </figure> <p>The right panel is the same four thousand draws, plotted differently: $u_n$ on the horizontal axis, and $9u_n - 6u_{n+1} + u_{n+2}$ on the vertical one. Every single point lands on one of fifteen horizontal lines, at integer heights. Not approximate! Exactly, to floating-point precision!</p> <p>If you are curious, the reason is three lines of algebra. Notice that $a - 3 = 65536 = 2^{16}$, so</p> \[(a-3)^2 = 2^{32} = 2 \cdot 2^{31} \equiv 0 \pmod{2^{31}},\] <p>which expands to $a^2 - 6a + 9 \equiv 0 \pmod{m}$. Since RANDU has no increment, $X_{n+1} = aX_n \bmod m$ and $X_{n+2} = a^2 X_n \bmod m$, so for every $n$:</p> \[X_{n+2} - 6X_{n+1} + 9X_n \equiv (a^2 - 6a + 9)\,X_n \equiv 0 \pmod{m}.\] <p>That’s an <em>exact</em> linear relation among any three consecutive outputs, baked into the choice of $a$. It forces every triple $(u_n, u_{n+1}, u_{n+2})$ onto one of a handful of parallel planes in the unit cube, a count you can work out from how far $9u_n - 6u_{n+1} + u_{n+2}$ can range, which is exactly fifteen. George Marsaglia published this in 1968, a decade after RANDU had shipped inside statistical packages used across the sciences.</p> <p>I think the RANDU story usually gets told wrong, if at all, as a cautionary tale about one bad multiplier. The actual failure is that an entire scientific community spent a decade treating “passes the tests we happened to run” as equivalent to “is fine,” and those aren’t the same claim unless your tests are exhaustive, which they never are. You could object that this is unfair. Bounding a generator’s behavior in every dimension it might ever get used in isn’t computationally possible, so some amount of <code class="language-plaintext highlighter-rouge">trust-until-broken</code> is unavoidable. That’s true, and it doesn’t change what you should do with it, which is treat “nothing’s broken yet” as a status report, not a guarantee.</p> <h2 id="passing-a-test-battery-is-not-proof-of-anything">Passing a test battery is not proof of anything</h2> <p>The modern version of “checking anyone thought to run” is <a href="https://www.iro.umontreal.ca/~simul/testu01/tu01.html">TestU01</a>, Pierre L’Ecuyer’s suite of statistical tests, bundled into batteries of increasing severity: SmallCrush, Crush, and BigCrush. The last one runs over a hundred distinct tests, among them birthday spacings, linear complexity, and matrix rank, each looking for a different flavor of structure a generator’s output shouldn’t have.</p> <p>“Passing BigCrush” means something weaker than it sounds. There are no test in that specific collection that has detected a deviation from uniform, at whatever significance threshold you chose, on the sequences you happened to feed it. You should know that this is not a proof that the generator is free of exploitable structure, only that the structure isn’t one of the roughly hundred kinds anyone has written a test for yet. RANDU would have failed almost every test in BigCrush catastrophically. But the tests that existed in the 1960s were mostly one- and two-dimensional, which were exactly the dimensions in which RANDU looks fine.</p> <h2 id="what-do-people-use-now">What do people use now?</h2> <p>Currently, to my knowledge, three generators dominate current practice. They trade off state size, speed, and how much scrutiny they’ve survived.</p> <p>The <strong>Mersenne Twister</strong> (MT19937) has an enormous period, $2^{19937}-1$, and was the default generator in Python, R, and NumPy for years. It has two soft spots. Its 624 words of internal state can be reconstructed exactly from 624 consecutive outputs, so it has no place near anything adversarial, and it fails a handful of BigCrush’s tests, mostly ones sensitive to linear structure, a smaller and subtler echo of RANDU’s own problem.</p> <p><strong>PCG</strong> (permuted congruential generator) runs an ordinary LCG internally, then applies a permutation to the output before you see it, which erases the linear structure that gives LCGs away. It passes BigCrush, needs far less state than MT19937, and is what <code class="language-plaintext highlighter-rouge">numpy.random.default_rng()</code> gives you today!</p> <p>The <strong>xoshiro/xoroshiro</strong> family (Blackman and Vigna) generates by shifting, rotating, and XOR-ing a small register, which makes it fast with only 128–256 bits of state, and it also clears BigCrush.</p> <p>Here’s the practical upshot - If you take one thing from this section: prefer <code class="language-plaintext highlighter-rouge">rng = np.random.default_rng(seed)</code> and pass <code class="language-plaintext highlighter-rouge">rng</code> around explicitly, over the older global <code class="language-plaintext highlighter-rouge">np.random.seed(seed)</code>. A single shared global generator means any library you import that draws numbers behind your back (and several will) perturbs a stream you thought was yours. And if you ever need several independent streams at once, parallel chains, say, both PCG and xoshiro support jumping ahead to a distant point in the sequence rather than juggling multiple arbitrary seeds.</p> <h2 id="why-not-just-use-real-randomness">Why not just use real randomness?</h2> <p>Believe it or not, your computer has access to unpredictable entropy: thermal noise sampled by <code class="language-plaintext highlighter-rouge">/dev/random</code>, the CPU’s <code class="language-plaintext highlighter-rouge">RDRAND</code> instruction, purpose-built hardware like quantum random number generators. It’s tempting to ask why we bother with elaborate deterministic recurrences when a real physical process is sitting right there.</p> <p>It’s because switching to it costs you reproducibility. You can’t replay a specific run or a specific bad sample for debugging, because the source doesn’t remember what it produced, and most hardware entropy pools are too slow or rate-limited to feed a Monte Carlo loop anyway. But the mismatch runs deeper than convenience. What a Monte Carlo method needs is equidistribution across however many dimensions it uses at once (precisely the property TestU01 measures and PCG and xoshiro are engineered against). Hardware entropy sources are engineered against a different property, unpredictability to an adversary, which happens to get called “more random” in casual conversation without making it any better suited to this particular job than a generator that’s actually been checked against a hundred specific statistical tests.</p> <h2 id="where-this-leaves-us">Where this leaves us</h2> <p>Modern generators pass every test anyone has thought to run against them. So did RANDU, once. That’s not a reason for alarm: it’s an epistemic position a deterministic recurrence puts you in.</p> <p>The next post starts from a perfect, uncorrelated stream of these numbers and asks how to turn it into a sample from an arbitrary distribution. It turns out that even with a flawless generator, the obvious methods fail as you try to scale up the dimensions.</p>]]></content><author><name></name></author><category term="GenAI"/><category term="ml"/><category term="generative-modelling"/><category term="probability"/><category term="rng"/><summary type="html"><![CDATA[Every sample in this series is a deterministic function of uniform bits, where those bits come from and why that's fine.]]></summary></entry></feed>