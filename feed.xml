<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://debajyotis.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://debajyotis.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-13T14:03:23+00:00</updated><id>https://debajyotis.github.io/feed.xml</id><title type="html">blank</title><subtitle>ML research engineer working on generative models, multimodal post-training, and scientific ML. PhD in Physics and Machine Learning, University of Geneva. Based in Geneva. </subtitle><entry><title type="html">How Do We Sample Anything at All? (Part 2: Rejection, Flows, and GenAI)</title><link href="https://debajyotis.github.io/blog/2026/distributions/" rel="alternate" type="text/html" title="How Do We Sample Anything at All? (Part 2: Rejection, Flows, and GenAI)"/><published>2026-08-13T00:00:00+00:00</published><updated>2026-08-13T00:00:00+00:00</updated><id>https://debajyotis.github.io/blog/2026/distributions</id><content type="html" xml:base="https://debajyotis.github.io/blog/2026/distributions/"><![CDATA[<p>In <a href="/blog/2026/how_we_sample/">Part 1</a>, we answered a fundamental question: how do we turn deterministic computer bits into samples from a probability distribution?</p> <p>We saw that by running the cumulative distribution function (CDF) in reverse, we can take a simple uniform float $U \sim \mathcal{U}(0,1)$ and transform it into a sample from a target distribution via $X = F^{-1}(U)$.</p> <p>That process feels so seamless that you might wonder why sampling remains an active area of research. The catch is that we quietly hid the hardest step inside $F^{-1}$.</p> <h2 id="where-simple-inversion-runs-into-a-wall">Where Simple Inversion Runs into a Wall</h2> <p>For basic distributions like the exponential, isolating $x$ takes a few lines of basic high school algebra. But for many crucial distributions, the inverse CDF has no closed-form expression. The standard Gaussian distribution is a classic example:</p> \[p(x) = \frac{1}{\sqrt{2\pi\sigma^2}} \exp\left( -\frac{(x-\mu)^2}{2\sigma^2} \right)\] <p>While we can compute its integral numerically, there is no neat algebraic function for $F^{-1}(u)$. Calling a complex numerical approximation inside a loop millions of times gets slow very quickly.</p> <p>When direct inversion breaks down, we have to get creative. Instead of attempting to invert the target distribution directly, we construct indirect processes that output the target shape as a byproduct.</p> <h2 id="rejection-sampling-filtering-for-the-right-shape">Rejection Sampling: Filtering for the Right Shape</h2> <p>Imagine that I want samples from some awkward distribution $p(x)$, but I know how to sample from an easier distribution $q(x)$. Suppose I can draw points from $q$, and I know that $p(x) \leq Mq(x)$ for some constant $M$.</p> <p>Now I can play a slightly strange game:</p> <ol> <li>Sample a candidate $x$ from the easy distribution $q(x)$.</li> <li>Generate another uniform random number $U \sim \mathcal{U}(0,1)$.</li> <li>Keep the candidate only if $U &lt; \frac{p(x)}{Mq(x)}$. Otherwise, throw it away and try again.</li> </ol> <p>This sounds terribly inefficient, and in high dimensions, it often is. But look at what is happening geometrically: we are generating points under an easy curve and selectively carving away excess height until the remaining points match the shape of $p(x)$.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>First, sample candidate x ~ q(x)
│
▼
Draw uniform check U ~ Uniform(0,1)
│
▼
Is U &lt; p(x) / (M * q(x))?
├── No  ──► Reject and try again
└── Yes ──► Accept sample!

</code></pre></div></div> <p>Whether through direct inversion or geometric filtering, our strategy remains unchanged. We don’t always need to know how to directly generate the distribution we want; sometimes it is enough to generate something easy and then <strong>filter or transform it until the survivors have the right distribution.</strong></p> <h2 id="we-have-been-transforming-randomness-this-whole-time">We Have Been Transforming Randomness This Whole Time</h2> <p>At this point, let’s zoom out and review the underlying pattern:</p> \[\boxed{ \text{simple randomness} \rightarrow \text{transformation} \rightarrow \text{desired randomness} }\] <p>Our computer starts with a deterministic algorithm and an initial seed, producing pseudo-random bits. We scale those bits into a simple canonical distribution $U \sim \mathcal{U}(0,1)$, and then apply a deterministic mathematical transformation.</p> <p>This single realization unlocks the door to high-dimensional machine learning. Because once we move from one-dimensional statistics to complex multi-dimensional spaces, this exact observation becomes the engine driving modern generative modeling.</p> <h2 id="from-random-numbers-to-generative-models">From Random Numbers to Generative Models</h2> <p>So far we have been talking about a single scalar random variable. But nothing says our random input has to be one-dimensional. We can instead start with a vector of simple noise:</p> \[\mathbf{z} \sim p(\mathbf{z}) \quad \text{where, for instance,} \quad \mathbf{z} \sim \mathcal{N}(\mathbf{0}, \mathbf{I})\] <p>Now imagine that we replace our basic single-variable equation with a massive, parameterized neural network:</p> \[\mathbf{x} = f_\theta(\mathbf{z})\] <p>If $f_\theta$ is designed and trained correctly, a simple Gaussian blob in latent space can be stretched, folded, and warped into a highly intricate distribution over images, text, or audio waveforms.</p> <p>This is the central concept behind <strong>normalising flows</strong>. Instead of learning how to directly sample from a complicated multi-modal image distribution, we learn an invertible neural network that maps a simple Gaussian distribution straight into it:</p> \[\mathbf{z} \sim p_Z(\mathbf{z}) \quad \longrightarrow \quad \mathbf{x} = f(\mathbf{z}) \sim p_X(\mathbf{x})\] <p>This is inverse transform sampling grown up. In one dimension, our transformation was $x = F^{-1}(u)$. In a normalising flow, we learn a high-dimensional equivalent of that same mapping function.</p> <p>And the story doesn’t stop there. Other generative frameworks use different mechanics to solve the exact same core problem:</p> <ul> <li><strong>Generative Adversarial Networks (GANs)</strong> train a generator network to transform a simple random noise vector into realistic outputs, using a discriminator network to guide the shape of that transformation.</li> <li><strong>Diffusion Models</strong> take a step-by-step route, gradually transforming pure Gaussian noise into structured data over hundreds of small denoising steps.</li> </ul> <p>The algorithms look very different on the surface, but the fundamental conceptual question remains identical across all of them:</p> <blockquote> <p><strong>How can I transform simple randomness into the kind of complex randomness I actually want?</strong></p> </blockquote> <h2 id="the-whole-story-in-one-picture">The Whole Story in One Picture</h2> <p>We can now map out the complete journey across both posts:</p> \[\boxed{ \text{random bits} \rightarrow \text{PRNG} \rightarrow U\sim\mathcal{U}(0,1) \rightarrow \text{Transformation } f(\mathbf{z}) \rightarrow \mathbf{X}\sim p(\mathbf{x}) }\] <p>The first step produces something that behaves randomly. The second step gives us a convenient canonical distribution like the uniform or standard normal. The final step—whether handled by an inverse CDF, a rejection filter, or a 100-million-parameter neural network—turns that plain noise into structured samples.</p> <p>That is the essence of sampling. Once you see it as a geometry transformation problem, the vast landscape of modern generative AI starts to look like elegant variations on the exact same game.</p>]]></content><author><name></name></author><category term="GenAI"/><category term="ml"/><category term="generative-modelling"/><category term="deep-learning"/><summary type="html"><![CDATA[Moving from simple inverse transform sampling to rejection sampling, multi-dimensional distributions, and modern generative AI models.]]></summary></entry><entry><title type="html">How Do We Sample Anything at All? (Part 1: The Inversion Trick)</title><link href="https://debajyotis.github.io/blog/2026/how_we_sample/" rel="alternate" type="text/html" title="How Do We Sample Anything at All? (Part 1: The Inversion Trick)"/><published>2026-08-13T00:00:00+00:00</published><updated>2026-08-13T00:00:00+00:00</updated><id>https://debajyotis.github.io/blog/2026/how_we_sample</id><content type="html" xml:base="https://debajyotis.github.io/blog/2026/how_we_sample/"><![CDATA[<p>There is an unassuming snippet of code that quietly sneaks its way into almost every machine learning repository on the planet:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>

<span class="n">rng</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">default_rng</span><span class="p">(</span><span class="mi">42</span><span class="p">)</span>
<span class="n">x</span> <span class="o">=</span> <span class="n">rng</span><span class="p">.</span><span class="nf">random</span><span class="p">()</span>
</code></pre></div></div> <p>Just like that, we have a random number. But if we pause to ask a slightly annoying question, the magic starts to unravel: <strong>where on earth did that number actually come from?</strong></p> <p>Computers are fundamentally deterministic machines. Hand a processor the exact same inputs alongside the exact same internal state, and it will give you the exact same outputs every single time. So how does a completely deterministic box of silicon produce something we can honestly call “random”?</p> <p>Even if we solve that puzzle, a bigger one is immediately waiting for us in the wings. Suppose you do not want a simple decimal between 0 and 1. What if you need to draw samples from a Gaussian curve, an exponential decay, or some absurd custom distribution you just scribbled on a napkin? Your random number generator only gives you uniform floats between 0 and 1. How do you bend, stretch, and fold those plain decimals until they look like samples from the distribution you actually care about?</p> <p>In this post, we are going to answer that question from first principles. We will start with essentially nothing: a deterministic computer and a humble stream of random bits, building up to a general recipe for transforming simple random numbers into samples from known probability distributions.</p> <h2 id="computers-are-secretly-predictable">Computers Are Secretly Predictable</h2> <p>Let’s begin with an uncomfortable truth: computers do not naturally produce randomness. A standard program executes instructions with absolute determinism. If you evaluate <code class="language-plaintext highlighter-rouge">x = f(42)</code>, assuming <code class="language-plaintext highlighter-rouge">f</code> has no sneaky side effects or external network reads, you will get the exact same <code class="language-plaintext highlighter-rouge">x</code> today, tomorrow, and heat-death-of-the-universe years from now.</p> <p>When NumPy effortlessly hands you a decimal via <code class="language-plaintext highlighter-rouge">rng.random()</code>, there isn’t a tiny atmospheric noise detector sitting on your CPU. It is running an algorithm. In fact, if you re-initialize the generator with <code class="language-plaintext highlighter-rouge">np.random.default_rng(42)</code> and call <code class="language-plaintext highlighter-rouge">rng.random()</code> again, you will get the exact same value down to the last digit.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Run this as many times as you like, the output never changes
</span><span class="n">rng</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">default_rng</span><span class="p">(</span><span class="mi">42</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="n">rng</span><span class="p">.</span><span class="nf">random</span><span class="p">())</span>  <span class="c1"># Always 0.7739560485559809
</span></code></pre></div></div> <p>This is why we call these systems <strong>pseudo-random number generators</strong> (PRNGs). They take a starting point, known as a seed, and deterministically compute a sequence of numbers that mimics the statistical behavior of pure noise. For almost every practical application, we do not need fundamental quantum randomness; we just need numbers that pass statistical tests for independence and uniformity.</p> <p>Far from being a drawback, this predictability is actually a superpower in machine learning and data science. Setting a seed ensures that your experiment is completely reproducible. If your model achieves brilliant results with seed 42, anyone else on earth can run your code and verify the exact same results. Reproducibility beats philosophical purity every single time.</p> <h2 id="asking-the-right-thing-from-a-random-generator">Asking the Right Thing from a Random Generator</h2> <p>Before tackling complex distributions, let’s look at what the basic random number generator gives us out of the box: a single number picked uniformly between 0 and 1.</p> <p>Mathematically, we write this as $U \sim \mathcal{U}(0,1)$. The notation looks fancy, but the underlying concept is straightforward. $U$ is a random variable where every equal-width sub-interval inside $[0,1]$ has an identical chance of receiving a point. For instance, the probability of a draw landing between 0.0 and 0.1 is exactly 10%, the chance of landing between 0.4 and 0.5 is 10%, and the chance of landing between 0.9 and 1.0 is also 10%.</p> \[P(0 \leq U &lt; 0.1) = P(0.4 \leq U &lt; 0.5) = P(0.9 \leq U \leq 1.0) = 0.1\] <p>If you sample a million points from this generator and plot them as a histogram, you will see a flat, featureless block:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">samples</span> <span class="o">=</span> <span class="n">rng</span><span class="p">.</span><span class="nf">random</span><span class="p">(</span><span class="mi">1_000_000</span><span class="p">)</span>
</code></pre></div></div> <p>Individual draws are unpredictable, but en masse, they reveal a perfectly flat structure. This distinction highlights something crucial about probability: a random variable isn’t interesting because an individual point feels unpredictable; it is interesting because <strong>the overall collection of samples obeys a specific shape</strong>.</p> <p>Which brings us back to our core problem: what if we need a shape that isn’t flat?</p> <h2 id="finding-a-ruler-for-probability">Finding a Ruler for Probability</h2> <p>Suppose someone hands you a target probability distribution $p(x)$ that rises in some places and falls in others. There are regions where points should cluster densely and quiet valleys where points rarely land. To draw samples correctly, your output values must land in those peaks and valleys with precise mathematical probabilities.</p> <p>To do that, we need a way to describe those probabilities quantitatively. The most common tool is the <strong>probability density function</strong> (PDF), written as $p(x)$. For continuous variables, the PDF describes how probability mass is spread across the real line, meaning the probability of a sample falling within a tiny window around $x$ is approximately $p(x)\mathrm{d}x$.</p> <p>While the PDF gives a great visual profile of a distribution, a different representation turns out to be vastly more useful for sampling: the <strong>cumulative distribution function</strong> (CDF), defined as $F_X(x) = P(X \leq x)$.</p> \[F_X(x) = P(X \leq x)\] <p>You can think of the CDF as sliding a vertical bar across your distribution from left to right, sweeping up probability mass as it goes. On the far left, you have gathered nothing, so $F_X(x) \approx 0$. By the time your sliding bar reaches the far right, you have swept up all the probability, making $F_X(x) \approx 1$.</p> <p>Notice what just happened. The CDF takes any physical value $x$ from your distribution and maps it to an output number between 0 and 1. Meanwhile, our basic computer random generator spits out values between 0 and 1. That structural symmetry is not a coincidence; it is the master key to sampling.</p> <h2 id="running-the-machine-in-reverse">Running the Machine in Reverse</h2> <p>Let’s revisit the standard uniform distribution $U \sim \mathcal{U}(0,1)$. Because probability accumulates at a constant rate across the unit interval, its cumulative distribution function is astonishingly simple: $F_U(u) = u$. If you plug in $u = 0.73$, the CDF simply tells you that 73% of the total probability lies to the left of 0.73.</p> <p>Now, let’s flip the perspective. What if, instead of asking “how much probability accumulates up to $x$?”, we ask the question in reverse: <strong>“At what coordinate $x$ does our accumulated probability reach 73%?”</strong></p> <p>For the uniform distribution, the answer is trivially 0.73. But when you apply this exact same reverse lookup to <em>any</em> continuous distribution, something remarkable happens.</p> \[X = F_X^{-1}(U)\] <p>This strategy is known as <strong>inverse transform sampling</strong>, and its execution is straightforward:</p> <ol> <li>Draw a uniform random number $U \sim \mathcal{U}(0,1)$ from your computer’s PRNG.</li> <li>Pass that value into the inverse cumulative distribution function $F_X^{-1}(U)$.</li> <li>The resulting output $X$ will be a perfectly valid sample drawn from $p(x)$.</li> </ol> <p>Why does this work so cleanly? The inverse CDF acts as an elastic ruler that stretches and compresses uniform probability levels. Regions of the target distribution containing 10% of the total mass get allocated exactly 10% of the uniform inputs. Steeper parts of the CDF compress uniform inputs into tight clusters, while flat regions spread them out over wider spans.</p> <p>We took an elementary, uniform decimal, fed it into a deterministic mathematical function, and produced a sample from a complex target distribution. Sampling isn’t about creating random mass out of thin air; <strong>sampling is a geometry transformation problem.</strong></p> <h2 id="a-concrete-example-the-exponential-distribution">A Concrete Example: The Exponential Distribution</h2> <p>To see inverse transform sampling in action, let me walk you through building an exponential sampler from scratch. The exponential distribution is widely used to model wait times between independent events, and its PDF is given by $p(x) = \lambda e^{-\lambda x}$ for $x \geq 0$.</p> <p>Integrating this density gives us a smooth, closed-form CDF: $F(x) = 1 - e^{-\lambda x}$. To find the inverse function, we set $u = 1 - e^{-\lambda x}$ and solve directly for $x$:</p> \[\begin{aligned} u &amp;= 1 - e^{-\lambda x} \\ e^{-\lambda x} &amp;= 1 - u \\ -\lambda x &amp;= \log(1 - u) \\ x &amp;= -\frac{1}{\lambda} \log(1 - u) \end{aligned}\] <p>Because $U$ is uniformly distributed on $[0,1]$, the quantity $1 - U$ is also uniformly distributed on $[0,1]$, allowing us to simplify the expression even further. In Python, our exponential generator boils down to a single line:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>

<span class="k">def</span> <span class="nf">sample_exponential</span><span class="p">(</span><span class="n">lambda_param</span><span class="p">:</span> <span class="nb">float</span><span class="p">,</span> <span class="n">size</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">1</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">np</span><span class="p">.</span><span class="n">ndarray</span><span class="p">:</span>
    <span class="n">u</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">default_rng</span><span class="p">().</span><span class="nf">random</span><span class="p">(</span><span class="n">size</span><span class="p">)</span>
    <span class="k">return</span> <span class="o">-</span><span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">u</span><span class="p">)</span> <span class="o">/</span> <span class="n">lambda_param</span>
</code></pre></div></div> <p>If you draw a million points with this function and plot their histogram, you will recover an exponential decay curve. The target distribution didn’t exist inside Python’s random generator; we forged it by warping a uniform line through a logarithm.</p> <h2 id="the-journey-so-far">The Journey So Far</h2> <p>We have built a working pipeline from raw deterministic silicon all the way to controlled probability distributions:</p> \[\boxed{ \text{random bits} \rightarrow \text{PRNG} \rightarrow U\sim\mathcal{U}(0,1) \rightarrow F^{-1} \rightarrow X\sim p(x) }\] <p>This algebraic trick works brilliantly whenever we can write down $F^{-1}$ explicitly. But what happens when the math gets messy, the inverse CDF has no closed form, or we want to sample complex high-dimensional objects like images?</p> <p>In <strong>Part 2</strong>, we will explore how rejection sampling saves us when inversion fails, and how this simple idea of transforming randomness directly powers modern generative AI models.</p>]]></content><author><name></name></author><category term="GenAI"/><category term="ml"/><category term="generative-modelling"/><category term="probability"/><summary type="html"><![CDATA[A friendly exploration of how deterministic computers generate probability distributions from scratch using inverse transform sampling.]]></summary></entry></feed>