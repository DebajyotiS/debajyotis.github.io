<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://debajyotis.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://debajyotis.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-14T10:15:18+00:00</updated><id>https://debajyotis.github.io/feed.xml</id><title type="html">blank</title><subtitle>ML research engineer working on generative models, multimodal post-training, and scientific ML. PhD in Physics and Machine Learning, University of Geneva. Based in Geneva. </subtitle><entry><title type="html">How Do We Sample Anything at All? (Part 2: What If We Can’t Sample Directly?)</title><link href="https://debajyotis.github.io/blog/2026/mcmc-sampling/" rel="alternate" type="text/html" title="How Do We Sample Anything at All? (Part 2: What If We Can’t Sample Directly?)"/><published>2026-08-14T00:00:00+00:00</published><updated>2026-08-14T00:00:00+00:00</updated><id>https://debajyotis.github.io/blog/2026/mcmc-sampling</id><content type="html" xml:base="https://debajyotis.github.io/blog/2026/mcmc-sampling/"><![CDATA[<p>In <a href="/blog/2026/inverse-transform-sampling/">Part 1 of our sampling series</a>, we built a complete pipeline from raw deterministic silicon to probability distributions. We saw that if you have a uniform random number $U \sim \mathcal{U}(0,1)$, you can pass it through an inverse cumulative distribution function $X = F^{-1}(U)$ to get a sample from your target distribution. When direct inversion gets tricky, rejection sampling lets you generate points under an easy curve and filter out the excess height.</p> <p>That pipeline works beautifully as long as you can evaluate the full distribution and invert its CDF. But in the real world, especially in Bayesian statistics, we constantly run into a far more stubborn problem.</p> <p>What happens when you have a function that tells you how probable a state is, but you have no way to sample from it directly?</p> <h2 id="the-bayesian-trap-intractable-normalizing-constants">The Bayesian Trap: Intractable Normalizing Constants</h2> <p>To see why this happens, look at the foundation of Bayesian inference:</p> \[p(\theta \mid y) = \frac{p(y \mid \theta) p(\theta)}{p(y)}\] <p>We want to sample parameters $\theta$ given some observed data $y$. The numerator is straightforward: $p(y \mid \theta)$ is the likelihood of our data, and $p(\theta)$ is our prior belief. We can evaluate both easily for any specific choice of $\theta$.</p> <p>The trouble lives in the denominator. The marginal likelihood $p(y)$, also known as the evidence, requires integrating over every possible value of $\theta$:</p> \[p(y) = \int p(y \mid \theta) p(\theta) \, \mathrm{d}\theta\] <p>In high-dimensional spaces, evaluating this integral analytically is completely impossible. We are left with a proportional relationship:</p> \[p(\theta \mid y) \propto p(y \mid \theta) p(\theta)\] <p>We can evaluate the relative height of the target distribution at any coordinate, but we do not know the normalizing constant that makes the total area under the curve integrate to 1. Without that constant, the CDF $F(\theta)$ is unavailable, rendering inverse transform sampling completely useless.</p> <p>This forces us to ask a radically different question: <strong>can we generate valid samples from a probability distribution if all we can do is evaluate its unnormalized height?</strong></p> <h2 id="first-fallback-rejection-sampling">First Fallback: Rejection Sampling</h2> <p>Part 1 flagged rejection sampling as the natural next tool once inversion fails, so let’s see how far it actually gets us. The recipe is simple: pick an easy-to-sample proposal (or “envelope”) distribution $q(x)$, scale it by a constant $M$ large enough that it sits above the target everywhere, $p(x) \leq M q(x)$, and then play a filtering game:</p> <ol> <li>Draw a candidate $x$ from the easy distribution $q(x)$.</li> <li>Draw a uniform random number $U \sim \mathcal{U}(0,1)$.</li> <li>Accept $x$ if $U &lt; \dfrac{p(x)}{M q(x)}$. Otherwise, reject it and draw another candidate.</li> </ol> <p>Geometrically, you’re scattering points uniformly under the curve $Mq(x)$ and keeping only the ones that also fall under $p(x)$. What survives that filter is, by construction, distributed exactly like $p(x)$. This still works even when $p(x)$ is only known up to a constant, since that missing constant just gets folded into $M$.</p> <p>So why not reach for this every time inversion fails? Two problems compound quickly:</p> <ul> <li><strong>You need to already know the shape you’re trying to sample.</strong> Choosing a valid $M$ means bounding $p(x)$ everywhere, including in tails you haven’t explored yet. For a black-box density, nobody hands you that bound for free.</li> <li><strong>Acceptance collapses as dimensionality grows.</strong> The acceptance rate is roughly the ratio of the volume under $p(x)$ to the volume under $Mq(x)$. Even a modest number of dimensions can shrink that ratio so far that you throw away nearly every candidate you draw.</li> </ul> <p>Rejection sampling still asks you to out-guess the target distribution before you’ve sampled it. What we actually want is a method that learns the shape of $p(x)$ as it explores, spending its time automatically where the density is high with no envelope to design at all.</p> <h2 id="constructing-an-ugly-distribution">Constructing an Ugly Distribution</h2> <p>To visualize this challenge, imagine you are handed a bumpy bimodal target distribution made of two separated Gaussians:</p> \[p(x) = 0.3 \, \mathcal{N}(-2, 0.5^2) + 0.7 \, \mathcal{N}(2, 1^2)\] <table> <thead> <tr> <th style="text-align: left">Region</th> <th style="text-align: left">Left Peak</th> <th style="text-align: left">Saddle / Valley</th> <th style="text-align: left">Right Peak</th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><strong>Location ($x$)</strong></td> <td style="text-align: left">$x = -2.0$</td> <td style="text-align: left">$x = 0.0$</td> <td style="text-align: left">$x = 2.0$</td> </tr> <tr> <td style="text-align: left"><strong>Relative Density $p(x)$</strong></td> <td style="text-align: left">Moderate Peak</td> <td style="text-align: left">Very Low Density</td> <td style="text-align: left">High Peak</td> </tr> </tbody> </table> <p>Imagine you do not know this comes from two Gaussians. All you possess is a black-box function <code class="language-plaintext highlighter-rouge">unnormalized_density(x)</code> that returns a single height number for any $x$ you pass in. You cannot invert it, and you cannot easily bound it with an envelope for rejection sampling.</p> <p>If you cannot draw samples directly, what if you built an exploratory walk that naturally spends more time in high-density regions?</p> <h2 id="step-1-the-dumb-random-walk">Step 1: The Dumb Random Walk</h2> <p>Let’s start with the simplest exploratory process you could possibly code: a blind random walk. Pick a starting point $x_0$, and at each step, propose a new position by adding a bit of Gaussian noise:</p> \[x_{t+1} = x_t + \epsilon, \qquad \epsilon \sim \mathcal{N}(0, \sigma^2)\] <p>If you run this process for 10,000 steps and plot a histogram of where the walker traveled, you will get a scattered mess that depends entirely on where you started and how long you ran the code.</p> <p>The walker wanders around, but it has no awareness of $p(x)$. It treats low-probability valleys and high-probability peaks with identical curiosity. To turn this random walk into a valid sampler, we need a mechanism that biases the walker toward regions where $p(x)$ is large.</p> <h2 id="step-2-the-metropolis-algorithm">Step 2: The Metropolis Algorithm</h2> <p>In 1953, Nicholas Metropolis and his collaborators introduced an ingenious rule: accept every proposed move that goes uphill, but occasionally accept moves that go downhill.</p> <p>The algorithm proceeds as follows:</p> <ol> <li>Start at a current state $x_t$.</li> <li>Propose a candidate state $x’$ using a symmetric proposal step $x’ \sim \mathcal{N}(x_t, \sigma^2)$.</li> <li>Calculate the acceptance ratio:</li> </ol> \[\alpha = \min\left(1, \frac{p(x')}{p(x_t)}\right)\] <ol> <li>Draw a uniform random number $U \sim \mathcal{U}(0,1)$. If $U &lt; \alpha$, accept the proposal and set $x_{t+1} = x’$. Otherwise, reject the proposal and stay put: $x_{t+1} = x_t$.</li> </ol> <pre><code class="language-mermaid">flowchart TD
    A["Current position: x_t"] --&gt; B["Propose candidate: x'"]
    B --&gt; C["α = min(1, p(x')/p(x_t))"]
    C --&gt; D{"Draw U ~ Uniform(0,1)\nIs U &lt; α?"}
    D --&gt;|Yes| E["Accept: x_(t+1) = x'"]
    D --&gt;|No| F["Reject: x_(t+1) = x_t"]
</code></pre> <h3 id="why-downhill-moves-matter">Why Downhill Moves Matter</h3> <p>The crucial intuition lies in how downhill moves are handled. If a proposed point $x’$ has lower probability than $x_t$, we do not automatically reject it. We accept it with probability $\alpha = \frac{p(x’)}{p(x_t)}$.</p> <p>That downhill tolerance is deliberate. If your algorithm only accepted uphill moves, it would turn into an optimization routine. It would climb to the nearest local peak, get stuck there forever, and mistake a single mode for the entire probability distribution. The occasional downhill step is what allows the chain to descend into valleys, cross low-density regions, and discover other peaks across the landscape.</p> <h3 id="watching-it-sample-the-bimodal-target">Watching It Sample the Bimodal Target</h3> <p>If you let this Metropolis chain step around for enough iterations, the histogram of its path converges to match $p(x)$ with remarkable precision. Here it is on our bimodal target from earlier, using no CDF, no envelope, just the accept/reject rule:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>

<span class="k">def</span> <span class="nf">unnormalized_density</span><span class="p">(</span><span class="n">x</span><span class="p">):</span>
    <span class="n">left</span> <span class="o">=</span> <span class="mf">0.3</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="o">-</span><span class="mf">0.5</span> <span class="o">*</span> <span class="p">((</span><span class="n">x</span> <span class="o">+</span> <span class="mi">2</span><span class="p">)</span> <span class="o">/</span> <span class="mf">0.5</span><span class="p">)</span> <span class="o">**</span> <span class="mi">2</span><span class="p">)</span>
    <span class="n">right</span> <span class="o">=</span> <span class="mf">0.7</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="o">-</span><span class="mf">0.5</span> <span class="o">*</span> <span class="p">((</span><span class="n">x</span> <span class="o">-</span> <span class="mi">2</span><span class="p">)</span> <span class="o">/</span> <span class="mf">1.0</span><span class="p">)</span> <span class="o">**</span> <span class="mi">2</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">left</span> <span class="o">+</span> <span class="n">right</span>

<span class="k">def</span> <span class="nf">metropolis</span><span class="p">(</span><span class="n">n_steps</span><span class="p">,</span> <span class="n">x0</span><span class="o">=</span><span class="mf">0.0</span><span class="p">,</span> <span class="n">sigma</span><span class="o">=</span><span class="mf">1.0</span><span class="p">,</span> <span class="n">seed</span><span class="o">=</span><span class="mi">0</span><span class="p">):</span>
    <span class="n">rng</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">default_rng</span><span class="p">(</span><span class="n">seed</span><span class="p">)</span>
    <span class="n">x</span> <span class="o">=</span> <span class="n">x0</span>
    <span class="n">samples</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">empty</span><span class="p">(</span><span class="n">n_steps</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n_steps</span><span class="p">):</span>
        <span class="n">proposal</span> <span class="o">=</span> <span class="n">x</span> <span class="o">+</span> <span class="n">rng</span><span class="p">.</span><span class="nf">normal</span><span class="p">(</span><span class="mf">0.0</span><span class="p">,</span> <span class="n">sigma</span><span class="p">)</span>
        <span class="n">alpha</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="mf">1.0</span><span class="p">,</span> <span class="nf">unnormalized_density</span><span class="p">(</span><span class="n">proposal</span><span class="p">)</span> <span class="o">/</span> <span class="nf">unnormalized_density</span><span class="p">(</span><span class="n">x</span><span class="p">))</span>
        <span class="k">if</span> <span class="n">rng</span><span class="p">.</span><span class="nf">random</span><span class="p">()</span> <span class="o">&lt;</span> <span class="n">alpha</span><span class="p">:</span>
            <span class="n">x</span> <span class="o">=</span> <span class="n">proposal</span>
        <span class="n">samples</span><span class="p">[</span><span class="n">t</span><span class="p">]</span> <span class="o">=</span> <span class="n">x</span>
    <span class="k">return</span> <span class="n">samples</span>

<span class="n">samples</span> <span class="o">=</span> <span class="nf">metropolis</span><span class="p">(</span><span class="n">n_steps</span><span class="o">=</span><span class="mi">10_000</span><span class="p">)</span>
<span class="c1"># A histogram of `samples` recovers the 0.3 / 0.7 split between the two
# peaks, even though unnormalized_density() was never normalized and its
# CDF was never written down.
</span></code></pre></div></div> <p>Notice everything this chain <em>didn’t</em> need: no inverse CDF, no envelope $q(x)$, no bound $M$. It only ever compares $p(x)$ at two points, which is exactly why it survives contact with an unnormalized posterior.</p> <h2 id="why-does-metropolis-work-detailed-balance">Why Does Metropolis Work? (Detailed Balance)</h2> <p>The reason this simple game produces valid samples comes down to a property called <strong>detailed balance</strong>.</p> <p>Because our proposal step is symmetric, the probability of proposing a move from $x$ to $x’$ is identical to proposing a move from $x’$ back to $x$: $q(x’ \mid x) = q(x \mid x’)$. Combined with our acceptance rule, the transition probabilities satisfy a strict equilibrium condition:</p> \[p(x) P(x \rightarrow x') = p(x') P(x' \rightarrow x)\] <p>This equation states that in long-run equilibrium, the rate of probability flowing from state $x$ to state $x’$ exactly balances the probability flowing from $x’$ back to $x$.</p> <p>You do not need a dense theoretical treatise on Markov chains to appreciate the core consequence: <strong>the target distribution $p(x)$ becomes the unique stationary distribution of the process.</strong></p> <p>This yields the central philosophy of Markov Chain Monte Carlo:</p> \[\boxed{ \text{Don't sample from } p(x) \text{ directly. Build a process whose stationary distribution is } p(x). }\] <h2 id="metropolis-hastings-and-the-bayesian-payoff">Metropolis-Hastings and the Bayesian Payoff</h2> <p>In 1970, W.K. Hastings generalized the Metropolis algorithm to handle asymmetric proposal distributions $q(x’ \mid x)$, where proposing a step from $x$ to $x’$ might be easier than proposing the return trip.</p> <p>Adjusting the acceptance ratio to account for this asymmetry yields the <strong>Metropolis-Hastings</strong> acceptance probability:</p> \[\alpha = \min\left(1, \frac{p(x') q(x_t \mid x')}{p(x_t) q(x' \mid x_t)}\right)\] <p>Now, observe what happens when we plug our unnormalized Bayesian posterior into this ratio:</p> \[\frac{p(\theta' \mid y)}{p(\theta_t \mid y)} = \frac{\left( \frac{p(y \mid \theta') p(\theta')}{p(y)} \right)}{\left( \frac{p(y \mid \theta_t) p(\theta_t)}{p(y)} \right)} = \frac{p(y \mid \theta') p(\theta')}{p(y \mid \theta_t) p(\theta_t)}\] <p>Look at that cancellation: <strong>the intractable evidence term $p(y)$ completely disappears.</strong></p> <p>Because Metropolis-Hastings depends exclusively on the ratio of probabilities between two points, the unknown normalizing constant cancels out entirely. We can sample complex Bayesian posteriors without ever computing the impossible denominator integral.</p> <h2 id="gibbs-sampling-dividing-multi-dimensional-problems">Gibbs Sampling: Dividing Multi-Dimensional Problems</h2> <p>When moving to high-dimensional spaces $\mathbf{x} = (x_1, x_2, \ldots, x_d)$, proposing random moves for every variable simultaneously leads to low acceptance rates.</p> <p><strong>Gibbs sampling</strong> simplifies this by updating one variable at a time, conditioned on the current values of all other variables:</p> <ol> <li>Sample $x_1^{(t+1)} \sim p(x_1 \mid x_2^{(t)}, x_3^{(t)}, \ldots, x_d^{(t)})$</li> <li>Sample $x_2^{(t+1)} \sim p(x_2 \mid x_1^{(t+1)}, x_3^{(t)}, \ldots, x_d^{(t)})$</li> <li>Repeat for all $d$ dimensions.</li> </ol> <p>The neat feature of Gibbs sampling is that every single conditional proposal is automatically accepted: $\alpha = 1$. By breaking a joint distribution down into a series of one-dimensional updates, you eliminate rejected proposals entirely.</p> <p>However, Gibbs sampling hits a severe bottleneck when variables are strongly correlated. If $x_1$ and $x_2$ form a tight diagonal ridge, updating along individual coordinate axes forces the sampler to take tiny zig-zag steps, causing exploration to crawl to a near standstill.</p> <h2 id="the-random-walk-bottleneck">The Random Walk Bottleneck</h2> <p>While Metropolis-Hastings and Gibbs sampling solve the conceptual challenge of sampling without a normalizing constant, simple random-walk proposals scale poorly to high dimensions.</p> <table> <thead> <tr> <th style="text-align: left">Problem</th> <th style="text-align: left">Cause</th> <th style="text-align: left">Practical Impact</th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><strong>High Rejections</strong></td> <td style="text-align: left">Large step size $\sigma$</td> <td style="text-align: left">Proposals land in low-probability regions and get rejected.</td> </tr> <tr> <td style="text-align: left"><strong>Slow Diffusion</strong></td> <td style="text-align: left">Small step size $\sigma$</td> <td style="text-align: left">Chain accepts moves, but explores via slow random walks.</td> </tr> <tr> <td style="text-align: left"><strong>Correlation</strong></td> <td style="text-align: left">Off-diagonal covariance</td> <td style="text-align: left">Sampler gets stuck taking micro-steps along narrow ridges.</td> </tr> </tbody> </table> <p>Random walks suffer from the fundamental physics of diffusion: to cover a distance $D$, a random walk requires roughly $D^2$ steps. In a 500-dimensional space, blind random walks waste almost all their time rejecting proposals or taking tiny steps in circles.</p> <p>To scale up, we need samplers that use the geometry of the target space to guide their steps.</p> <h2 id="gradient-based-sampling-from-langevin-dynamics-to-mala">Gradient-Based Sampling: From Langevin Dynamics to MALA</h2> <p>Instead of proposing moves blindly, we can compute the gradient of the log-density: $\nabla_x \log p(x)$. This vector points directly in the direction where probability density increases.</p> <p>Using this geometric signal gives us <strong>Unadjusted Langevin Algorithm (ULA)</strong> steps:</p> \[x_{t+1} = x_t + \frac{\epsilon^2}{2} \nabla_x \log p(x_t) + \epsilon \mathbf{z}, \qquad \mathbf{z} \sim \mathcal{N}(\mathbf{0}, \mathbf{I})\] <p>This equation combines a deterministic nudge uphill along the gradient with a controlled injection of random noise $\mathbf{z}$.</p> <p>Because discrete step sizes introduce integration drift, we combine this update with a Metropolis accept/reject check. This combination is called the <strong>Metropolis-Adjusted Langevin Algorithm (MALA)</strong>. By using gradients, MALA proposes moves that actively follow the contours of the target distribution, yielding far higher acceptance rates over larger distances.</p> <h2 id="hamiltonian-monte-carlo-hmc-turning-probability-into-physics">Hamiltonian Monte Carlo (HMC): Turning Probability Into Physics</h2> <p>If MALA is like walking uphill with a compass, <strong>Hamiltonian Monte Carlo (HMC)</strong> is like turning the probability landscape into a frictionless skate park.</p> <p>Instead of treating $x$ as a point floating in space, HMC imagines $x$ as the position of a physical particle sliding over a surface. We introduce an auxiliary momentum vector $\mathbf{p} \sim \mathcal{N}(\mathbf{0}, \mathbf{M})$ and define a physical system with total energy $H(\mathbf{x}, \mathbf{p})$:</p> \[H(\mathbf{x}, \mathbf{p}) = \underbrace{-\log p(\mathbf{x})}_{\text{Potential Energy } U(\mathbf{x})} + \underbrace{\frac{1}{2} \mathbf{p}^\top \mathbf{M}^{-1} \mathbf{p}}_{\text{Kinetic Energy } K(\mathbf{p})}\] <p>High-density regions in $p(\mathbf{x})$ act as deep gravity wells (low potential energy), while low-density regions act as steep hills (high potential energy).</p> <p>To propose a new state, HMC hands the particle a random momentum kick $\mathbf{p}$ and simulates its trajectory over continuous time using Hamilton’s equations:</p> \[\frac{\mathrm{d}\mathbf{x}}{\mathrm{d}t} = \frac{\partial H}{\partial \mathbf{p}}, \qquad \frac{\mathrm{d}\mathbf{p}}{\mathrm{d}t} = -\frac{\partial H}{\partial \mathbf{x}}\] <figure> <picture> <img src="/assets/img/sampling-series/hmc-potential-well.svg" class="img-fluid rounded" width="100%" height="auto" alt="Potential energy landscape U(x) = -log p(x) for the bimodal target, with a particle trajectory sliding from the saddle into the deeper right-hand well." loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">The same bimodal target from earlier, now viewed as a potential-energy landscape. A momentum kick at the saddle sends the particle sliding into the deeper well.</figcaption> </figure> <p>Using a numerical integration scheme called the leapfrog integrator, the particle glides across the landscape, converting potential energy into kinetic energy as it sweeps through high-density zones, before climbing up the far side of the well.</p> <p>Because physical systems conserve total energy $H(\mathbf{x}, \mathbf{p})$, the proposed destination point along this trajectory maintains a near-100% acceptance probability, even over large distances in high-dimensional spaces.</p> <p>While random-walk MCMC stumbles around blindly, Hamiltonian Monte Carlo uses physics and gradients to trace trajectories through complex target distributions.</p> <h2 id="practical-limitations-of-mcmc">Practical Limitations of MCMC</h2> <p>Despite their power, MCMC algorithms are not silver bullets. Working with MCMC requires managing several practical constraints:</p> <ul> <li><strong>Burn-in / Warm-up:</strong> The chain begins at an arbitrary starting point $x_0$. Early samples reflect this arbitrary initial position rather than the target distribution, and must be discarded.</li> <li><strong>Autocorrelation:</strong> Consecutive samples $x_t$ and $x_{t+1}$ drawn from a chain are structurally correlated, violating the assumption of independent and identically distributed draws.</li> <li><strong>Mixing Speed:</strong> If a target distribution contains isolated modes separated by deep regions of near-zero density, MCMC chains can get trapped in a single mode for thousands of iterations.</li> <li><strong>Effective Sample Size (ESS):</strong> A chain of 100,000 correlated MCMC steps might contain the same statistical information as only 1,000 independent samples.</li> </ul> <h2 id="looking-ahead">Looking Ahead</h2> <p>We have built an arsenal of tools for generating samples. In Part 1, we inverted simple CDFs. In Part 2, we tried an envelope first, then constructed Markov chains that converge to complex, unnormalized target distributions.</p> <p>On its own, a pile of samples is just numbers in a vector. <strong>Part 3</strong> asks what all that trouble was actually for: it turns out drawing samples lets us compute high-dimensional integrals, estimate expectations, and solve problems that would otherwise be computationally intractable.</p>]]></content><author><name></name></author><category term="GenAI"/><category term="ml"/><category term="generative-modelling"/><category term="mcmc"/><category term="bayesian"/><summary type="html"><![CDATA[Exploring Markov Chain Monte Carlo, Metropolis-Hastings, and Hamiltonian Monte Carlo when direct sampling and inversion fail.]]></summary></entry><entry><title type="html">How Do We Sample Anything at All? (Part 1: The Inversion Trick)</title><link href="https://debajyotis.github.io/blog/2026/inverse-transform-sampling/" rel="alternate" type="text/html" title="How Do We Sample Anything at All? (Part 1: The Inversion Trick)"/><published>2026-08-13T00:00:00+00:00</published><updated>2026-08-13T00:00:00+00:00</updated><id>https://debajyotis.github.io/blog/2026/inverse-transform-sampling</id><content type="html" xml:base="https://debajyotis.github.io/blog/2026/inverse-transform-sampling/"><![CDATA[<p>There is an unassuming snippet of code that sneaks its way into almost every machine learning repository on the planet:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>

<span class="n">rng</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">default_rng</span><span class="p">(</span><span class="mi">42</span><span class="p">)</span>
<span class="n">x</span> <span class="o">=</span> <span class="n">rng</span><span class="p">.</span><span class="nf">random</span><span class="p">()</span>
</code></pre></div></div> <p>Just like that, we have a random number. But if we pause to ask a slightly annoying question, the magic starts to unravel: <strong>where on earth did that number actually come from?</strong></p> <p>Computers are fundamentally deterministic machines. Hand a processor the exact same inputs alongside the exact same internal state, and it will give you the exact same outputs every single time. So how does a completely deterministic box of silicon produce something we can honestly call “random”?</p> <p>Even if we solve that puzzle, a bigger one is immediately waiting for us in the wings. Suppose you do not want a simple decimal between 0 and 1, but samples from a Gaussian curve, an exponential decay, or some absurd custom distribution you just scribbled on a napkin. Your random number generator only gives you uniform floats between 0 and 1, so somehow those plain decimals need to be bent, stretched, and folded until they look like samples from the distribution you actually care about.</p> <p>In this post, we are going to answer that question from first principles. We will start with essentially nothing: a deterministic computer and a humble stream of random bits, building up to a general recipe for transforming simple random numbers into samples from known probability distributions.</p> <h2 id="computers-are-secretly-predictable">Computers Are Secretly Predictable</h2> <p>Let’s begin with an uncomfortable truth: computers do not naturally produce randomness. A standard program executes instructions with absolute determinism. If you evaluate <code class="language-plaintext highlighter-rouge">x = f(42)</code>, assuming <code class="language-plaintext highlighter-rouge">f</code> has no sneaky side effects or external network reads, you will get the exact same <code class="language-plaintext highlighter-rouge">x</code> today, tomorrow, and heat-death-of-the-universe years from now.</p> <p>When NumPy effortlessly hands you a decimal via <code class="language-plaintext highlighter-rouge">rng.random()</code>, there isn’t a tiny atmospheric noise detector sitting on your CPU. It is running an algorithm. In fact, if you re-initialize the generator with <code class="language-plaintext highlighter-rouge">np.random.default_rng(42)</code> and call <code class="language-plaintext highlighter-rouge">rng.random()</code> again, you will get the exact same value down to the last digit.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Run this as many times as you like, the output never changes
</span><span class="n">rng</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">default_rng</span><span class="p">(</span><span class="mi">42</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="n">rng</span><span class="p">.</span><span class="nf">random</span><span class="p">())</span>  <span class="c1"># Always 0.7739560485559633
</span></code></pre></div></div> <p>This is why we call these systems <strong>pseudo-random number generators</strong> (PRNGs). They take a starting point, known as a seed, and deterministically compute a sequence of numbers that mimics the statistical behavior of pure noise. For almost every practical application, numbers that pass statistical tests for independence and uniformity work just as well as fundamental quantum randomness.</p> <p>Far from being a drawback, this predictability is actually a superpower in machine learning and data science. Setting a seed ensures that your experiment is completely reproducible. If your model achieves brilliant results with seed 42, anyone else on earth can run your code and verify the exact same results. Reproducibility beats philosophical purity every single time.</p> <h2 id="asking-the-right-thing-from-a-random-generator">Asking the Right Thing from a Random Generator</h2> <p>Before tackling complex distributions, let’s look at what the basic random number generator gives us out of the box: a single number picked uniformly between 0 and 1.</p> <p>Mathematically, we write this as $U \sim \mathcal{U}(0,1)$. The notation looks fancy, but the underlying concept is straightforward. $U$ is a random variable where every equal-width sub-interval inside $[0,1]$ has an identical chance of receiving a point. For instance, the probability of a draw landing between 0.0 and 0.1 is exactly 10%, the chance of landing between 0.4 and 0.5 is 10%, and the chance of landing between 0.9 and 1.0 is also 10%.</p> \[P(0 \leq U &lt; 0.1) = P(0.4 \leq U &lt; 0.5) = P(0.9 \leq U \leq 1.0) = 0.1\] <p>If you sample a million points from this generator and plot them as a histogram, you will see a flat, featureless block:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">samples</span> <span class="o">=</span> <span class="n">rng</span><span class="p">.</span><span class="nf">random</span><span class="p">(</span><span class="mi">1_000_000</span><span class="p">)</span>
</code></pre></div></div> <p>Individual draws are unpredictable, but en masse, they reveal a perfectly flat structure. This distinction highlights something crucial about probability: what makes a random variable interesting is <strong>the specific shape the overall collection of samples obeys</strong>, not whether any single point feels unpredictable.</p> <p>The problem we actually care about starts where that flatness ends: what does it take to produce a shape that isn’t flat?</p> <h2 id="finding-a-ruler-for-probability">Finding a Ruler for Probability</h2> <p>Suppose someone hands you a target probability distribution $p(x)$ that rises in some places and falls in others. There are regions where points should cluster densely and quiet valleys where points rarely land. To draw samples correctly, your output values must land in those peaks and valleys with precise mathematical probabilities.</p> <p>To do that, we need a way to describe those probabilities quantitatively. The most common tool is the <strong>probability density function</strong> (PDF), written as $p(x)$. For continuous variables, the PDF describes how probability mass is spread across the real line, meaning the probability of a sample falling within a tiny window around $x$ is approximately $p(x)\mathrm{d}x$.</p> <p>While the PDF gives a great visual profile of a distribution, a different representation turns out to be vastly more useful for sampling: the <strong>cumulative distribution function</strong> (CDF), defined as $F_X(x) = P(X \leq x)$.</p> \[F_X(x) = P(X \leq x)\] <p>You can think of the CDF as sliding a vertical bar across your distribution from left to right, sweeping up probability mass as it goes. On the far left, you have gathered nothing, so $F_X(x) \approx 0$. By the time your sliding bar reaches the far right, you have swept up all the probability, making $F_X(x) \approx 1$.</p> <p>Notice what just happened. The CDF takes any physical value $x$ from your distribution and maps it to an output number between 0 and 1. Meanwhile, our basic computer random generator spits out values between 0 and 1. That structural symmetry is the master key to sampling.</p> <h2 id="running-the-machine-in-reverse">Running the Machine in Reverse</h2> <p>Let’s revisit the standard uniform distribution $U \sim \mathcal{U}(0,1)$. Because probability accumulates at a constant rate across the unit interval, its cumulative distribution function is astonishingly simple: $F_U(u) = u$. If you plug in $u = 0.73$, the CDF simply tells you that 73% of the total probability lies to the left of 0.73.</p> <p>Now, let’s flip the perspective. What if, instead of asking “how much probability accumulates up to $x$?”, we ask the question in reverse: <strong>“At what coordinate $x$ does our accumulated probability reach 73%?”</strong></p> <p>For the uniform distribution, the answer is trivially 0.73. But when you apply this exact same reverse lookup to <em>any</em> continuous distribution, something remarkable happens.</p> \[X = F_X^{-1}(U)\] <p>This strategy is known as <strong>inverse transform sampling</strong>, and its execution is straightforward:</p> <ol> <li>Draw a uniform random number $U \sim \mathcal{U}(0,1)$ from your computer’s PRNG.</li> <li>Pass that value into the inverse cumulative distribution function $F_X^{-1}(U)$.</li> <li>The resulting output $X$ will be a perfectly valid sample drawn from $p(x)$.</li> </ol> <p>The inverse CDF works so cleanly because it acts as an elastic ruler that stretches and compresses uniform probability levels. Regions of the target distribution containing 10% of the total mass get allocated exactly 10% of the uniform inputs. Steeper parts of the CDF compress uniform inputs into tight clusters, while flat regions spread them out over wider spans.</p> <p>We took an elementary, uniform decimal, fed it into a deterministic mathematical function, and produced a sample from a complex target distribution. <strong>Sampling is a geometry transformation problem</strong>, not an act of conjuring randomness out of thin air.</p> <h2 id="a-concrete-example-the-exponential-distribution">A Concrete Example: The Exponential Distribution</h2> <p>To see inverse transform sampling in action, let me walk you through building an exponential sampler from scratch. The exponential distribution is widely used to model wait times between independent events, and its PDF is given by $p(x) = \lambda e^{-\lambda x}$ for $x \geq 0$.</p> <p>Integrating this density gives us a smooth, closed-form CDF: $F(x) = 1 - e^{-\lambda x}$. To find the inverse function, we set $u = 1 - e^{-\lambda x}$ and solve directly for $x$:</p> \[\begin{aligned} u &amp;= 1 - e^{-\lambda x} \\ e^{-\lambda x} &amp;= 1 - u \\ -\lambda x &amp;= \log(1 - u) \\ x &amp;= -\frac{1}{\lambda} \log(1 - u) \end{aligned}\] <p>Because $U$ is uniformly distributed on $[0,1]$, the quantity $1 - U$ is also uniformly distributed on $[0,1]$, allowing us to simplify the expression even further. In Python, our exponential generator boils down to a single line:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>

<span class="k">def</span> <span class="nf">sample_exponential</span><span class="p">(</span><span class="n">lambda_param</span><span class="p">:</span> <span class="nb">float</span><span class="p">,</span> <span class="n">size</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">1</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">np</span><span class="p">.</span><span class="n">ndarray</span><span class="p">:</span>
    <span class="n">u</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">default_rng</span><span class="p">().</span><span class="nf">random</span><span class="p">(</span><span class="n">size</span><span class="p">)</span>
    <span class="k">return</span> <span class="o">-</span><span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">u</span><span class="p">)</span> <span class="o">/</span> <span class="n">lambda_param</span>
</code></pre></div></div> <p>If you draw a million points with this function and plot their histogram, you will recover an exponential decay curve. The target distribution never existed inside Python’s random generator. We forged it by warping a uniform line through a logarithm.</p> <h2 id="the-journey-so-far">The Journey So Far</h2> <p>We have built a working pipeline from raw deterministic silicon all the way to controlled probability distributions:</p> \[\boxed{ \text{random bits} \rightarrow \text{PRNG} \rightarrow U\sim\mathcal{U}(0,1) \rightarrow F^{-1} \rightarrow X\sim p(x) }\] <p>This algebraic trick works brilliantly whenever we can write down $F^{-1}$ explicitly. But what happens when the math gets messy, the inverse CDF has no closed form, or we want to sample complex high-dimensional objects like images?</p> <p>In <strong>Part 2</strong>, we will explore how rejection sampling saves us when inversion fails, and how this simple idea of transforming randomness directly powers modern generative AI models.</p>]]></content><author><name></name></author><category term="GenAI"/><category term="ml"/><category term="generative-modelling"/><category term="probability"/><summary type="html"><![CDATA[A friendly exploration of how deterministic computers generate probability distributions from scratch using inverse transform sampling.]]></summary></entry></feed>